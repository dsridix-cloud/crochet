<section class="customer-reset-password-section" style="padding: 64px 0 96px; background: var(--color-bg);">
  <div class="container" style="max-width: 500px;">
    
    <div style="background: #FFF; border-radius: var(--radius-xl); border: 1px solid var(--color-beige); padding: 36px 32px; box-shadow: var(--shadow-subtle);">
      <div style="text-align: center; margin-bottom: 24px;">
        <h1 class="heading-serif" style="font-size: 24px;">{{ 'customer.reset_password.title' | t }}</h1>
        <p style="font-size: 13px; color: rgba(51,44,40,0.7); margin-top: 4px;">{{ 'customer.reset_password.subtext' | t: email: email }}</p>
      </div>

      {% form 'reset_customer_password' %}
        {{ form.errors | default_errors }}

        <div style="display: flex; flex-direction: column; gap: 16px;">
          <div>
            <label for="password" style="font-size: 12px; font-weight: 700; text-transform: uppercase; display: block; margin-bottom: 6px;">{{ 'customer.reset_password.password' | t }}</label>
            <input type="password" name="customer[password]" id="password" required class="form-input">
          </div>

          <div>
            <label for="password_confirmation" style="font-size: 12px; font-weight: 700; text-transform: uppercase; display: block; margin-bottom: 6px;">{{ 'customer.reset_password.password_confirm' | t }}</label>
            <input type="password" name="customer[password_confirmation]" id="password_confirmation" required class="form-input">
          </div>

          <button type="submit" class="btn btn-primary" style="padding: 14px; font-size: 12px; justify-content: center; width: 100%;">
            <span>{{ 'customer.reset_password.submit' | t }}</span>
          </button>
        </div>
      {% endform %}
    </div>

  </div>
</section>

{% schema %}
{
  "name": "Reset Password",
  "settings": []
}
{% endschema %}
